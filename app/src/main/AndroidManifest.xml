<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizmaker2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Train.TrainResultActivity"></activity>
        <activity android:name=".Quiz.AddQuiz.AddQuestionActivity" />
        <activity android:name=".Train.TrainActivity" />
        <activity
            android:name=".Main.Fragment.Home.AddQuiz"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Quiz.QuizMainActivity" />
        <activity android:name=".Test.ResultActivity" />
        <activity android:name=".Test.TestActivity" />
        <activity
            android:name=".Main.Main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login.RegisterActivity" />
        <activity
            android:name=".Login.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>